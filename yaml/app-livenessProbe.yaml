apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-demo-app-liveness
  labels:
    app: go-demo-app-liveness
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-demo-app-liveness
  template:
    metadata:
      labels:
        app: go-demo-app-liveness
    spec:
      containers:
        - name: go-demo-app
          image: den-vasyliev/go-demo-app:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
